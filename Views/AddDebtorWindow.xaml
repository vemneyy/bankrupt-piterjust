<!-- Views/AddDebtorWindow.xaml -->
<Window x:Class="bankrupt_piterjust.Views.AddDebtorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Добавить нового должника" Height="300" Width="400"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <StackPanel Margin="20">
        <TextBlock Text="ФИО должника:"/>
        <TextBox x:Name="FullNameTextBox" Margin="0,5,0,10"/>

        <TextBlock Text="Субъект РФ:"/>
        <TextBox x:Name="RegionTextBox" Margin="0,5,0,10"/>

        <TextBlock Text="Статус (текст на кнопке):"/>
        <TextBox x:Name="StatusTextBox" Margin="0,5,0,20" Text="Подать заявление"/>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Добавить" Width="100" Margin="0,0,10,0" IsDefault="True" Click="AddButton_Click"/>
            <Button Content="Отмена" Width="100" IsCancel="True"/>
        </StackPanel>
    </StackPanel>
</Window>