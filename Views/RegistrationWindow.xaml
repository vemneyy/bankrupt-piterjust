<Window x:Class="bankrupt_piterjust.Views.RegistrationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:bankrupt_piterjust.ViewModels"
        mc:Ignorable="d"
        Title="Регистрация" Height="500" Width="400"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="#F8F9FA" Icon="/Images/icon.png">
    <Window.DataContext>
        <vm:RegistrationViewModel/>
    </Window.DataContext>

    <Border Background="White" CornerRadius="6" Padding="25">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <TextBlock Text="Фамилия *" Margin="0,0,0,2"/>
                <TextBox Text="{Binding LastName, UpdateSourceTrigger=PropertyChanged}" Height="30" Margin="0,0,0,10"/>
                <TextBlock Text="Имя *" Margin="0,0,0,2"/>
                <TextBox Text="{Binding FirstName, UpdateSourceTrigger=PropertyChanged}" Height="30" Margin="0,0,0,10"/>
                <TextBlock Text="Отчество" Margin="0,0,0,2"/>
                <TextBox Text="{Binding MiddleName, UpdateSourceTrigger=PropertyChanged}" Height="30" Margin="0,0,0,10"/>
                <TextBlock Text="Должность *" Margin="0,0,0,2"/>
                <TextBox Text="{Binding Position, UpdateSourceTrigger=PropertyChanged}" Height="30" Margin="0,0,0,10"/>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                    <TextBlock Text="Пол:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <CheckBox Content="Мужской" IsChecked="{Binding IsMale}"/>
                </StackPanel>
                <TextBlock Text="Логин *" Margin="0,0,0,2"/>
                <TextBox Text="{Binding Login, UpdateSourceTrigger=PropertyChanged}" Height="30" Margin="0,0,0,10"/>
                <TextBlock Text="Пароль *" Margin="0,0,0,2"/>
                <PasswordBox Height="30" Margin="0,0,0,10" PasswordChanged="PasswordBox_PasswordChanged"/>
                <TextBlock Text="Телефон" Margin="0,0,0,2"/>
                <TextBox Text="{Binding Phone, UpdateSourceTrigger=PropertyChanged}" Height="30" Margin="0,0,0,10"/>
                <TextBlock Text="Email" Margin="0,0,0,2"/>
                <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Height="30" Margin="0,0,0,10"/>

                <Button Content="Зарегистрироваться" Height="40" Margin="0,10,0,10"
                        Command="{Binding RegisterCommand}"
                        IsEnabled="{Binding CanRegister}"/>
                <Button Content="Отмена" Height="30" Command="{Binding CancelCommand}"/>
            </StackPanel>
        </ScrollViewer>
    </Border>
</Window>
